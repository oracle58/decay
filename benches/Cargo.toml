[package]
name = "benches"
version = "0.1.0"
edition = "2024"
publish = false
autobenches = false

[dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
decay = { path = ".." }

[[bench]]
name = "render"
harness = false

[[bench]]
name = "ecs"
harness = false
